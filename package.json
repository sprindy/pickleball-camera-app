{
  "name": "pickleball-trail-camera",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "echo 'no linter configured'",
    "check:structure": "node scripts/check-structure.js"
  },
  "description": "iOS-only uni-app camera app with pickleball trajectory overlay"
}
